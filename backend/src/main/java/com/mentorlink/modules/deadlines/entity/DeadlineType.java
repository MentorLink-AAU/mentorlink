package com.mentorlink.modules.deadlines.entity;

/** Deadline classification: group formation, project submission, or progress update. */
public enum DeadlineType {
    GROUP_FORMATION,
    PROJECT_SUBMISSION,
    PROGRESS_UPDATE
}
