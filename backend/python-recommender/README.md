# Matrix Factorization Recommender

This folder contains the repo-local Python recommender used by MentorLink.

Entry point:

- `analysis/matrix_factorization_recommender.py`

Install dependencies:

```powershell
py -m pip install -r backend/python-recommender/analysis/requirements.txt
```
